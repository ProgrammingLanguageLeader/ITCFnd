import styled from 'styled-components';

const Logo = styled.img.attrs({
    alt: 'Logo'
})`
    max-width: 208px;
    max-height: 64px;
    margin-top: 50px;
    color: #fff;
`;

export default Logo;